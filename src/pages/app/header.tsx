import {
  IonHeader, IonTitle, IonToolbar,
} from "@ionic/react";

const Header: React.FC = () => <IonHeader>
  <IonToolbar>
    <IonTitle>Blank</IonTitle>
  </IonToolbar>
</IonHeader>;
export default Header;
